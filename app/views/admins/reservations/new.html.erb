<p hidden id='admin-new-resevation' class='pageName'>New Reservation</p>

<h1>New Reservation</h1>

<%= form_for( reservation, local: true,
              url: admins_reservations_path,  # MUST BE PLURAL
              id: "new-reservation-form", class: "reservation" ) do |form|  %>

<%= render 'form',  form: form, reservation: reservation,
                    spaces: spaces, events: events %>

<% end %>

<%# link_to 'Back', tenant_path(tenant, date: Calendar.display_date(date)) %>
